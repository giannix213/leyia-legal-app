// FirestoreExportPanel.js - Panel de exportación de Firestore
// Componente stub para evitar errores de importación

import React from 'react';

const FirestoreExportPanel = ({ organizacionId, onClose }) => {
  return (
    <div style={{ padding: '20px', border: '1px solid #ccc', borderRadius: '8px' }}>
      <h3>Panel de Exportación</h3>
      <p>Funcionalidad de exportación en desarrollo...</p>
      <p>Organización: {organizacionId}</p>
      <button onClick={onClose}>Cerrar</button>
    </div>
  );
};

export default FirestoreExportPanel;